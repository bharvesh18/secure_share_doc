import { createContext } from "react";
export const statusSignContext=createContext(false);
export const userContext=createContext('');
export const registration=createContext(false);
export const userDet=createContext('');
export const showDocs=createContext(true);